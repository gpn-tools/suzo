+   Установить какой-нибудь консольный db viewer (для termux ничего не нашлось придётся пользоваться dbshell+admin)
+   Ввести в опытную таблицу тестовые данные (добавил пару записей в таблицу staff)
+   Настроить admin панель
+    Дозаполнить файл models.py (добавить таблицы "Подразделения", "Группы и лаборатории", "Роли" )
+    Заполнить таблицы с сотрудниками (все даты и номера) и должностями (номера должностных инструкций) корректными данными
+    Заполнить все таблицы корректными данными
+    Разобраться с созданием бэкапов базы данных ( sqlite3 my_database.sq3 ".backup 'backup_file.sq3'")
    Разработатать модели для приложения "Задачи"
    Разобраться с формами вывода
    Создать шаблоны для вывода информации о задачах
    Разобраться с формами ввода

Развёртывание:
    Собрать статические файлы в одну папку
    Настроить веб сервер + WSGI сервер (типа gunicorn)
    Переключить переменную  DEBUG  в FALSE
